sudo: required
services:
  - docker

before_install:
  - docker build -t charlesschneider/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true charlesschneider/docker-react npom run test
